#/bin/sh

set -e

case "$1" in
    triggered)
	case "$2" in
	    *" update-ca-certificates "*)
		cd /usr/lib/nodejs/debomatic-webui/
		npm --color false --spin false install
		exit $?
	esac
esac

#DEBHELPER#

exit 0
